# Basic Example

Minimal example using `buildifier` from `buildifier_prebuilt`.

## Setup

```bash
bazel run //tools:dev
direnv allow
```

## Usage

Once set up, `buildifier` is available in your PATH:

```bash
buildifier --version
buildifier -r .
```
