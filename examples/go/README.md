# Go Example

Go development environment with `go` and `gazelle`.

## Setup

```bash
bazel run //tools:dev
direnv allow
```

## Usage

Once set up, `go` and `gazelle` are available in your PATH:

```bash
go version
gazelle --help
```
